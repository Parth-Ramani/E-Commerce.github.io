import { Container, Typography } from '@mui/material'
import { Grid } from 'mdi-material-ui'
import React from 'react'

const Footer = () => {
  return (
    <>
      <Container>
        <Grid container>
          <Grid item>
            <Typography variant='h1'>product</Typography>
            <Typography variant='h2'>Clothes</Typography>
            <Typography variant='h2'>Computers</Typography>
            <Typography variant='h2'>HardDisk</Typography>
            <Typography variant='h2'>Rings</Typography>
          </Grid>
          <Grid item>
            <Typography variant='h1'>product</Typography>
            <Typography variant='h2'>Clothes</Typography>
            <Typography variant='h2'>Computers</Typography>
            <Typography variant='h2'>HardDisk</Typography>
            <Typography variant='h2'>Rings</Typography>
          </Grid>
          <Grid item>
            <Typography variant='h1'>product</Typography>
            <Typography variant='h2'>Clothes</Typography>
            <Typography variant='h2'>Computers</Typography>
            <Typography variant='h2'>HardDisk</Typography>
            <Typography variant='h2'>Rings</Typography>
          </Grid>
          <Grid item>
            <Typography variant='h1'>product</Typography>
            <Typography variant='h2'>Clothes</Typography>
            <Typography variant='h2'>Computers</Typography>
            <Typography variant='h2'>HardDisk</Typography>
            <Typography variant='h2'>Rings</Typography>
          </Grid>
        </Grid>
      </Container>
    </>
  )
}

export default Footer
